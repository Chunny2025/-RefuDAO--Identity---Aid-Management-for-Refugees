[project]
name = 'RefuDAO--Identity---Aid-Management-for-Refugees'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.RefuDAO--Identity---Aid-Management-for-Refugees]
path = 'contracts/RefuDAO--Identity---Aid-Management-for-Refugees.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
